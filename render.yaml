services:
  # Backend web service
  - type: web
    name: portfolio-backend
    runtime: docker
    rootDir: backend
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "0"
      - key: ALLOWED_HOSTS
        value: ".onrender.com,localhost"
      - key: REDIS_URL
        fromService:
          type: redis
          name: portfolio-redis
          property: connectionString
    autoDeploy: true
    disk:
      name: sqlite-data
      mountPath: /app/data      # matches your SQLite directory (backend/data/)
      sizeGB: 1

  # Redis instance (required for Channels)
  - type: redis
    name: portfolio-redis
    plan: free
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all connections (backend will use this internally)